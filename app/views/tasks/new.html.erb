<h1>新規タスク登録</h1>
<%= form_for(@task) do |f| %>
<%= f.label :name, '名前' %>
<%= f.text_field :name %>
<%= f.collection_select :genre_id, Genre.all, :id, :name, include_blank: "--選択してください--" %>
<%= f.submit '作成' %>
<% end %>

